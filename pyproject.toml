[project]
name = "quant-finance-learning"
version = "0.1.0"
description = "A structured quantitative finance learning repository"
authors = [
    {name = "Your Name"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    # same list as requirements.txt
]

[project.optional-dependencies]
dev = [
    "pytest",
    "black",
    "ruff",
    "pre-commit"
]

[tool.black]
line-length = 88

[tool.ruff]
line-length = 88